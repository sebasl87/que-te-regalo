import {
  MD3LightTheme as DefaultTheme,
} from "react-native-paper";

const theme = {
  ...DefaultTheme,  
  colors: {
    ...DefaultTheme.colors,
    primary: "#F5B042",
    secondary: "70E1F5",
    tertiary: "#a1b2c3",    
  },
};
export default theme;
